# API reference

This page is a placeholder in Phase 4.

## Source of truth

The OpenAPI spec is generated by the backend in `apps/api`.

## Next step

In Phase 5 we will:

- export/sync the OpenAPI spec from `apps/api` into `apps/web`
- render a reference UI (ReDoc or Swagger UI) from that spec
- avoid manually duplicating endpoint reference docs

